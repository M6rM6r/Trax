[project]
name = "trax-ai"
version = "0.1.0"
description = "Trax retention intelligence service"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

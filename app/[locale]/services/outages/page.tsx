import MainLayout from "@/components/shared/MainLayout";

const Page = ({ params }: { params: { locale: string } }) => {
  return (
    <MainLayout>
      <></>;
    </MainLayout>
  );
};

export default Page;
